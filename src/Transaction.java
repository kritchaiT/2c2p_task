public record Transaction(String id, double amount, String status) {
    // That's it! 
    // Java automatically builds the constructor, getters, 
    // equals(), hashCode(), and toString() for you.
}
